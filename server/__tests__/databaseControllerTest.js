const request = requrie('supertest');
const databaseController = require('../controllers/databaseController');

const server = 'http://localhost:3000';

describe('databaseController test', () => {
  beforeAll((done) => {
    // const wallet = 
  })

  describe('GET request to getTransactions', () => {
    it('', async () => {
      const data = databaseController.getTransactions()
    })

  })
})